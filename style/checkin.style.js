import { createStyles } from "@material-ui/core";

export default () =>
  createStyles({
    root: {
      display: "grid",
      margin: 16,
    },
    dateWrapper: {
      margin: 16,
    },
    date: {
      marginLeft: 12,
    },
  });
